# 에이전트리아 배포 어빌리티 엔드포인트
AGENTRIA_API_URL=
AGENTRIA_API_KEY=

# Authorization 헤더가 아닌 다른 인증 헤더가 필요할 때만 설정
AGENTRIA_API_KEY_HEADER=Authorization

# direct: 배포된 API가 입력 JSON을 바로 받는 경우
# wrapped: { ability, input } 형태를 받는 경우
AGENTRIA_PAYLOAD_MODE=direct
AGENTRIA_ABILITY_NAME=today_fit_agent
